1.Print Fibonacci series using any programming language you prefer up to “n” numbers . ( n should be configurable. E.g. If we send the input as 3, it should print the first four numbers 0,1,1,2 . If we press 6 , then it should print 0,1, 1, 2, 3, 5 )
   
   #include<stdio.h>
     int main() {
	int f1=0,f2=1,sum,i,n;
	scanf("%d",&n);
	printf("%d %d ",f1,f2);
	i = 2;
	while(i<n){
		sum=f1+f2;
		f1=f2;
		f2=sum;
		printf("%d ",f2);
		i+=1;
	}
	return 0;
}
 
 
 





2.Print the output in Reverse


import java.io.*; 
  
class Reverse { 
      
    static void reverseFibonacci(int n) 
    { 
        int a[] = new int[n]; 
      
        a[0] = 0; 
        a[1] = 1; 
      
        for (int i = 2; i < n; i++) 
        { 
      
        
            a[i] = a[i - 2] + a[i - 1]; 
        } 
      
        for (int i = n - 1; i >= 0; i--)  
        { 
      
  
            System.out.print(a[i] +" "); 
        } 
    } 
    public static void main(String[] args) 
    { 
        int n = 5; 
        reverseFibonacci(n); 
      
    } 
} 




3.For each output, I would need the count of Even numbers and odd numbers.
   #include <stdio.h>
  #include <math.h>

int main()
{
    int T, i, j, k;
    scanf("%d", &T);

    for (i=0; i<T; i++)
    {
        int N, M, val;
        scanf("%d%d", &N,&M);
        val = abs(N-M)+1;

        int n,m;
        if (M>N) { n=N; m=M; }
        if (N>M) { n=M; m=N; }

        int b_tri, e_tri, even=0, odd=0;
        if (n%3==1) { b_tri=3; even++; odd+=2; } 
        if (n%3==2) { b_tri=2; odd+=2; }
        if (n%3==0) { b_tri=1; odd++;  }

        if (m%3==1) { e_tri=1; even++; } 
        if (m%3==2) { e_tri=2; odd++;  even++; }
        if (m%3==0) { e_tri=3; odd+=2; even++; }

        val-=(e_tri+b_tri);
        val/=3;

        for (j=0; j<val; j++)
        {
            even++;
            odd+=2;
        }

        printf("Case %d:\nOdd = %d\nEven = %d\n", i+1, odd, even);
    }

    return 0;
}






4.For each output , add 4 to even numbers and 3 to odd numbers and print the output ( Eg. If the output is 0,1, 1, 2, 3, 5 then post addition we should get 4,4,4,6,6,8)
 #include<stdio.h>
int main() 
{
	int num1,num2,n,i = 1,temp;
	scanf("%d",&n);
	num1 = 0;
	printf("%d ",num1 + 4);
	do
	{
		if(num2 % 2 == 0)
			printf("%d ",num2 + 4);
		else
			printf("%d ",num2 + 3);
		temp = num1 + num2;
		num1 = num2;
		num2 = temp;
		i += 1;
	}while(i < n);
	return 0;
}
